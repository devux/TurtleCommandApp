print "Welcome to turtle app"

instructFilter = 'f,f,f,r,l,l,f,r'
for index in range(len(instructArray)):
  if instructArray[index] == 'f':
	print "*** f"
  elif instructArray[index] == 'r':
	print "*** f"
  elif instructArray[index] == 'l':
    print "*** f"